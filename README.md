# 🎹 Виктор Аргонов: Симулятор Композитора-Трансгуманиста

JRPG-тамагочи-менеджер с ежедневными решениями. Вы — Виктор Аргонов, композитор, философ и биохакер во Владивостоке.

## 🔧 Как собрать игру

### Требования

- **Node.js** v18+ (рекомендуется v20+)
- **npm** v9+

### Установка и сборка

```bash
# 1. Клонировать/скачать проект

# 2. Установить зависимости
npm install

# 3. Собрать проект
npm run build

# 4. Готовая игра — dist/index.html (один файл!)
```

### Запуск в режиме разработки

```bash
npm run dev
# Откроется на http://localhost:5173
```

### Превью сборки

```bash
npm run preview
# Откроется на http://localhost:4173
```

### Результат сборки

Один файл `dist/index.html` (≈350 КБ). Всё встроено внутрь благодаря `vite-plugin-singlefile`.
Просто откройте `dist/index.html` в браузере — игра работает без сервера!

> ⚠️ **Важно:** Если добавляете PNG-ассеты (портреты, события), при сборке в singlefile они НЕ встраиваются автоматически. Для production с ассетами запускайте через `npm run preview` или любой HTTP-сервер (например `npx serve dist`).

---

## 📁 Структура проекта

```
├── index.html              ← HTML-шаблон
├── package.json            ← Зависимости
├── vite.config.ts          ← Конфигурация Vite (НЕ МЕНЯТЬ)
├── README.md               ← ← Вы здесь
│
├── src/
│   ├── main.tsx            ← Точка входа React
│   ├── App.tsx             ← Главный компонент (game loop)
│   ├── index.css           ← Глобальные стили
│   │
│   ├── gameData.ts         ← 🎯 ВСЕ ДАННЫЕ ИГРЫ (контент!)
│   ├── gameState.ts        ← ⚙️ Игровой движок (логика)
│   │
│   ├── components/
│   │   ├── ActivityPanel.tsx      ← Панель действий
│   │   ├── AudiencePanel.tsx      ← Панель аудитории
│   │   ├── BandPanel.tsx          ← Управление группой
│   │   ├── CharacterPanel.tsx     ← Взаимодействие с персонажами
│   │   ├── CharacterPortrait.tsx  ← Портреты (PNG + emoji fallback)
│   │   ├── EventModal.tsx         ← Модалка событий
│   │   ├── RelationsPanel.tsx     ← Панель отношений
│   │   ├── StatBar.tsx            ← Полоска характеристики
│   │   └── SubstancePanel.tsx     ← Панель веществ
│   │
│   └── utils/
│       └── assetLoader.ts  ← Загрузчик PNG-ассетов
│
└── public/
    └── assets/
        ├── README.md       ← Документация по ассетам
        ├── portraits/      ← PNG-портреты персонажей
        │   └── .gitkeep
        └── events/         ← PNG-иллюстрации событий
            └── .gitkeep
```

---

## 🎯 Как добавлять контент

Всё добавление контента — через **один файл**: `src/gameData.ts`.
Просто добавляйте записи в массивы. Не нужно менять логику!

### 1. Новый персонаж

Добавьте в массив `CHARACTERS[]`:

```typescript
{
  id: 'new_char',                    // уникальный ID
  name: 'Имя Фамилия',              // отображаемое имя
  portrait: '🎭',                    // эмодзи (фоллбэк если нет PNG)
  color: '#ff6b6b',                  // цвет темы
  desc: 'Описание персонажа',
  naturalRoles: ['solist', 'keys'],  // роли, где талант (×1.5 буфф)
  mismatchQuotes: {                  // цитаты при неподходящей роли (×0.3)
    drums: '"Я не барабанщик!"',
    manager: '"Деньги — зло!"',
  },
  baseRelation: 20,                  // начальные отношения (-128..128)
  traits: ['тег1', 'тег2'],
  perks: [
    { name: 'Перк', desc: 'Описание', effect: { someKey: 10 } },
  ],
  interactions: [
    {
      id: 'new_char_talk',
      name: 'Поговорить',
      emoji: '💬',
      desc: 'Описание',
      energyCost: 10,
      effects: { sanity: 5, creativity: 10 },
      relationChange: 8,
      message: 'Результат взаимодействия.',
      audienceEffects: { intellectuals: 2 },  // опционально
      requiredRelation: 20,                    // опционально — порог отношений
    },
  ],
}
```

Затем кинуть `new_char.png` в `public/assets/portraits/`.

### 2. Новое событие

Добавьте в массив `EVENTS[]`:

```typescript
{
  id: 'my_event',                     // уникальный ID
  title: 'Название события',
  emoji: '🎭',
  category: 'random',                 // random | character | opera | drugs | chain
  characterId: 'ariel',               // опционально — привязка к персонажу
  desc: 'Описание ситуации.',

  // УСЛОВИЯ (все опциональны — без условий = всегда доступно)
  condition: {
    flags: ['some_flag'],              // флаг должен быть установлен
    noFlags: ['block_flag'],           // флаг НЕ должен быть установлен
    inBand: ['ariel'],                 // персонаж в группе
    notInBand: ['sasha'],             // персонаж НЕ в группе
    minRelation: { ariel: 30 },       // отношения >= N
    maxRelation: { ariel: -10 },      // отношения <= N
    minDay: 5,                        // мин. день
    maxDay: 30,                       // макс. день
    minMoney: 10000,
    maxMoney: 500,
    minPopularity: 50,
    operaIndex: 1,                    // текущая опера (0 = первая)
    minOperaProgress: 50,
    minHealth: 20,
    maxHealth: 50,
    minSanity: 10,
    maxSanity: 30,
    hasAddiction: ['mephedrone'],     // зависимость от вещества
    totalDosesMin: 10,               // всего доз принято
  },

  isChainEvent: true,                 // только по триггеру (не случайно)
  unique: true,                       // только один раз

  choices: [
    {
      text: 'Вариант ответа',
      effects: { money: 5000, health: -10, sanity: 5, operaProgress: 3 },
      audienceEffects: { intellectuals: 5, haters: -2 },
      relationEffects: { ariel: 15, mukhin: -5 },
      message: 'Что произошло.',

      // МЕТА-ЭФФЕКТЫ
      setsFlags: ['new_flag'],         // установить флаг
      removesFlags: ['old_flag'],      // убрать флаг
      triggersEventId: 'next_event',   // запустить цепочку
      triggersDelay: 3,                // через N дней
    },
  ],
}
```

Затем кинуть `my_event.png` в `public/assets/events/`.

### 3. Новое вещество

Добавьте в массив `SUBSTANCES[]`:

```typescript
{
  id: 'new_drug',
  name: 'Название',
  emoji: '💊',
  tier: 'hard',                // light | medium | hard | extreme
  desc: 'Описание эффектов',
  cost: 500,
  effects: {
    focus: 20, creativity: 15,
    health: -10, sanity: -5,
    energy: 10, bladder: -15, bowel: -10,
  },
  overdoseThreshold: 2,        // доз до передоза
  addictiveness: 0.5,          // 0..1 скорость зависимости
  audienceReaction: {           // опционально
    biohackers: 3, haters: 2,
  },
}
```

### 4. Новая активность

Добавьте в массив `ACTIVITIES[]`:

```typescript
{
  id: 'new_activity',
  name: 'Название',
  emoji: '🎯',
  desc: 'Описание',
  effects: {
    operaProgress: 5, energy: -15, focus: -5,
    health: 3, sanity: -2, money: -200,
    bladder: -10, bowel: -5,
  },
  audienceEffects: { musicians: 3, schizos: 2 },
  requiredFocus: 30,            // опционально
  requiredEnergy: 10,           // опционально
}
```

### 5. Новая опера

Добавьте в массив `OPERAS[]`:

```typescript
{
  id: 'opera4',
  name: 'Название оперы',
  desc: 'Описание',
  requiredProgress: 300,
  rewards: { money: 80000, popularity: 100 },
  audienceReaction: { intellectuals: 30, musicians: 25, schizos: 20 },
}
```

### 6. Новая роль в группе

Добавьте в массив `BAND_ROLES[]`:

```typescript
{
  id: 'flute',
  name: 'Флейта',
  emoji: '🪈',
  desc: 'Описание роли',
  buffs: { creativity: 6, sanity: 4, popularity: 2 },
}
```

### 7. Новая группа аудитории

Добавьте в массив `AUDIENCE_GROUPS[]`:

```typescript
{
  id: 'philosophers',
  name: 'Философы',
  emoji: '🤔',
  desc: 'Любят глубокие мысли',
  baseSize: 30,
  donateRate: 0.8,
}
```

---

## 🖼️ PNG-ассеты

### Портреты персонажей

Путь: `public/assets/portraits/{id}.png`

| Файл | Персонаж |
|------|----------|
| `viktor.png` | Виктор Аргонов |
| `ariel.png` | Ариэль |
| `olivia.png` | Оливия Кибер |
| `mitrofanov.png` | Митрофанов |
| `mukhin.png` | Мухин |
| `dantesik.png` | Дантесик |
| `tomilov.png` | Томилов |
| `volaliel.png` | Волалиэль Волко |
| `zheka.png` | Жека |
| `prof_ivanov.png` | Проф. Иванов |
| `marina.png` | Марина |
| `hacker_bob.png` | Боб-хакер |
| `sasha.png` | Саша |

**Формат:** 256×256+ px, квадрат, PNG (лучше с прозрачным фоном)

### Иллюстрации событий

Путь: `public/assets/events/{id}.png`

**Формат:** 400×300+ px, PNG/JPG

### Как это работает

Компонент `CharacterPortrait` пытается загрузить `portraits/{id}.png`.
Если файл не найден → показывает эмодзи.
`EventModal` пытается загрузить `events/{id}.png`.
Если не найден → показывает портрет персонажа или эмодзи.

**Просто кинь PNG — и оно в игре!**

---

## 🎮 Механики игры

### Энергия и переутомление
- Каждое действие тратит энергию
- Когда энергия = 0, можно продолжать, но каждое действие наносит растущий урон:
  - 1-е действие: -5 HP, -2 рассудок
  - 2-е: -10 HP, -5 рассудок
  - 3-е: -15 HP, -7 рассудок (и т.д.)

### Персонажи
- 12 персонажей с уникальными перками и взаимодействиями
- Отношения от -128 до +128
- Можно назначить ЛЮБОГО на ЛЮБУЮ роль в группе
- Естественная предрасположенность → ×1.5 буффы
- Несоответствие → ×0.3 буффы + смешная цитата

### Вещества (4 тира)
- 🍵 Лёгкие: чай, кофе, энергетик
- 💊 Ноотропы: пирацетам, ноопепт, фенибут
- 💉 Рецептурные: модафинил, лирика, баклофен, микродоза
- ☠️ Тяжёлые: мефедрон, альфа-ПВП (КРАЙНЕ ОПАСНО)

### Аудитория
- 8 групп: интеллектуалы, шизы, хомяки, тролли, хейтеры, биохакеры, музыканты, нормисы
- Каждая приносит донаты в зависимости от размера и мнения
- Разные действия влияют на разные группы — невозможно угодить всем!

### Система событий
- Случайные события каждый день
- Цепочки событий с задержкой
- Уникальные события (один раз за игру)
- Персонажные серии (зависят от отношений, членства в группе, флагов)
- Оперные события (привязаны к прогрессу)
- Наркотические события (привязаны к зависимости)

### Сохранение/загрузка
- 💾 — сохранить (localStorage)
- 📂 — загрузить
- 🔄 — рестарт

---

## 🏗️ Архитектура

### Принцип: Universal Modular Core

```
gameData.ts (ДАННЫЕ)          gameState.ts (ЛОГИКА)         App.tsx (UI)
┌──────────────────┐      ┌──────────────────────┐     ┌─────────────┐
│ CHARACTERS[]     │      │ checkCondition()     │     │ State Loop  │
│ EVENTS[]         │──────│ getRandomEvent()     │─────│ Tabs/Panels │
│ SUBSTANCES[]     │      │ applyEffects()       │     │ EventModal  │
│ ACTIVITIES[]     │      │ processNight()       │     │ Save/Load   │
│ OPERAS[]         │      │ getBandBuffs()       │     └─────────────┘
│ BAND_ROLES[]     │      │ calculateDonations() │
│ AUDIENCE_GROUPS[]│      │ serialize/deserialize │
└──────────────────┘      └──────────────────────┘
        ↓                          ↓
    Добавляй сюда!        Не трогай (если не ломаешь)
```

**Чтобы добавить контент — меняй только `gameData.ts`.**
Логика (`gameState.ts`) и UI (`App.tsx`, компоненты) автоматически подхватят всё новое.

---

## 📝 Лицензия

Фан-проект. Не для коммерческого использования.
